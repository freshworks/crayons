{
  "name": "@freshworks-ui-kit/dropdown",
  "version": "0.1.0",
  "svelte": "src/index.html",
  "module": "dist/index.mjs",
  "main": "dist/index.js",
  "author": "Freshworks",
  "scripts": {
    "prepare": "yarn run build",
    "clean": "rimraf dist && mkdir dist",
    "build:rollup": "rollup src/dropdown.html --config ../../rollup.config.js --environment MODULE:index.mjs,MAIN:index.js,NAME:@freshworks-ui-kit/dropdown",
    "build": "yarn run clean && yarn run build:rollup"
  },
  "keywords": [
    "svelte"
  ],
  "files": [
    "src",
    "dist/index.mjs",
    "dist/index.js"
  ],
  "peerDependencies": {
    "svelte": "^2.15.3"
  }
}
