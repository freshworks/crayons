const screenshotTest = require('../../screenshotTest');
const setup = {
  avatar: [
    'default',
    'size-variants',
    'light-and-dark-mode-variants',
    'shape-variants',
  ],
};
const DELAY = 0;
screenshotTest(setup, DELAY);
