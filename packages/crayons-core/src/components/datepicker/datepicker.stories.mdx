import { Canvas, Meta, Story } from '@storybook/addon-docs';

import { within, userEvent } from '@storybook/testing-library';

export const Template = (args) =>{
  return `<fw-datepicker-uitest></fw-datepicker-uitest>`;
};

# Datepicker

<Meta
  title='Datepicker'
  component='fw-datepicker-uitest'
/>

## Props

<Props of="fw-datepicker-uitest"/>

## Usage

### Default

<Canvas>
  <Story
    name="Filled Form"
    play={async ({ canvasElement }) => {
      // Starts querying the component from it's root element
      setTimeout(() => {
        
        canvasElement.querySelector('fw-datepicker-uitest').testShowSingleDatePicker();
        //const datePicker = canvasElement.querySelector('fw-datepicker-uitest').shadowRoot.querySelector('fw-datepicker');
        //console.log(datePicker);
        //const rect = dataTable.getBoundingClientRect();
        //const x = rect.left + window.scrollX;
        //const y = rect.top + window.scrollY;
        //console.log(x,y);
        //document.elementFromPoint(x,y).click();
      },1000);
       
  }}>
  {Template.bind({})}
  </Story>
</Canvas>
