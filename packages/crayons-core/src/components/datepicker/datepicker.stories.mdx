import { html } from 'lit';
import { Meta, Story, Canvas } from '@storybook/web-components';

export const Template = () =>{
  return `<fw-datepicker-uitest value="2021-12-02"></fw-datepicker-uitest>`;
};


# Datepicker

<Meta
  title='Datepicker'
  component='fw-datepicker-uitest'
/>

## Props

<Props of="fw-datepicker-uitest"/>

## Usage

## datepicker open
<Preview>
  <Story 
    name='Default'
    play={ async ({ canvasElement }) => {
        const datePicker = document.querySelector('fw-datepicker-uitest');
        datePicker.testShowSingleDatePicker();
    }}
    decorators={[
      (Story) => {
        const story = Story();
        return `<div>${story}</div>`;
      }
    ]}
  >
    {Template.bind({})}
  </Story>
</Preview>

### datepicker month ddopen
<Preview>
  <Story 
    name='Datepicker Open'
    play={ async ({ canvasElement }) => {
      // Starts querying the component from it's root element
      const datePicker = document.querySelector('fw-datepicker-uitest');
      datePicker.testShowSingleDatePicker();
      setTimeout(() => {
        datePicker.shadowRoot.querySelector('fw-datepicker').shadowRoot.querySelector('fw-popover').querySelector('fw-select').shadowRoot.querySelector('fw-popover').querySelector('fw-button').click();
      },300);
    }}
    decorators={[
      (Story) => {
        const story = Story();
        return `<div>${story}</div>`;
      }
    ]}
  >
    {Template.bind({})}
  </Story>
</Preview>

