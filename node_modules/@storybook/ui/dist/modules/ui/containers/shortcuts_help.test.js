"use strict";

var _shortcuts_help = require("./shortcuts_help");

describe('manager.ui.containers.shortcuts_help', function () {
  describe('mapper', function () {
    test('should give correct data', function () {
      var toggleShortcutsHelp = function toggleShortcutsHelp() {
        return null;
      };

      var showShortcutsHelp = true;
      var props = {};
      var env = {
        actions: function actions() {
          return {
            ui: {
              toggleShortcutsHelp: toggleShortcutsHelp
            }
          };
        }
      };
      var state = {
        showShortcutsHelp: showShortcutsHelp
      };
      var result = (0, _shortcuts_help.mapper)(state, props, env);
      expect(result.isOpen).toBe(showShortcutsHelp);
      expect(result.onClose).toBe(toggleShortcutsHelp);
    });
  });
});