{
  "name": "freshworks-ui-kit",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "components/*"
  ],
  "devDependencies": {
    "@babel/core": "^7.2.0",
    "@babel/preset-env": "^7.2.0",
    "@storybook/addon-a11y": "^4.0.12",
    "@storybook/addon-actions": "^4.0.12",
    "@storybook/addon-knobs": "^4.1.3",
    "@storybook/addon-notes": "^4.1.2",
    "@storybook/html": "^4.0.12",
    "babel-loader": "^8.0.4",
    "lerna": "^3.6.0",
    "rollup": "^0.67.4",
    "rollup-plugin-svelte": "^4.4.0",
    "svelte": "^2.15.3",
    "svelte-loader": "^2.11.0",
    "webpack": "^4.27.1",
    "yarn": "^1.12.3"
  },
  "scripts": {
    "build:components": "lerna run build",
    "release": "lerna publish",

    "dev": "yarn build:components && kill-port 9001 && start-storybook -p 9001 -c .storybook",

    "dev": "yarn build:components && start-storybook -p 9001 -c .storybook",

    "build:storybook": "yarn build:components && build-storybook -c .storybook -o dist"
  }
}
